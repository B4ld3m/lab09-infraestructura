name: saludo
  on:
    push:
    branches:
    - main
    - develop
jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
    - name: Saludo desde main
      if: github.ref == 'refs/heads/main'
      run: echo "Hola desde main, mi nombre es Rodrigo Baldeon"
    - name: Saludo desde develop
      if: github.ref == 'refs/heads/develop'
      run: echo "Hola desde develop, mi nombre es Rodrigo Baldeon"
